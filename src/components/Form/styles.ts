import styled from "styled-components";

export const Wrapper = styled.form`
	display: flex;
	flex-direction: column;
	max-width: 100%;
	margin: auto;
	align-items: center;
	gap: 2rem;

	input {
		width: 350px;
	}
`;
